# The __init__.py file is usually empty and serves as an indicator to Python that the directory should be treated as a package.
# In this case, the file doesn't need to contain any code for our application.

""